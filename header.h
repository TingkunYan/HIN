#ifndef HEADER_H
#define HEADER_H
#include <iostream>
#include <vector>
#include <string>
#include <hash_set>
#include <fstream>
#include <sstream>
#include <set>
#include <map>
#include <stack>
#include <queue>
#include <hash_map>
#include <unordered_map>
#include <unordered_set>
#include <Eigen/Dense>
#include <locale.h>
#include <list>
#include <iterator>

#define ZERO 0.0f //这个参数控制着是否队列一直向下迭代，当概率非常的小时，不在往下迭代


typedef Eigen::MatrixXf myMatrix;
typedef Eigen::VectorXf myVector;

using namespace std;
#endif // !HEADER_H
#pragma once
